version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - echo 'pre-build'
    build:
      - echo 'build'
      - pip install -r requirements.txt
    post-build:
      - echo 'post-build'
run:
  runtime-version: 3.7.11
  command: mkdocs serve --no-livereload --verbose